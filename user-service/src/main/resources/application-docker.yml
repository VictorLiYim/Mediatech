spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/${DB_NAME:user_db}
    username: ${DB_USER:mediatheque}
    password: ${DB_PASSWORD:mediatheque}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
