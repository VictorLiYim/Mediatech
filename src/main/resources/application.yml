spring:
  application:
    name: mediatheque-service
  profiles:
    active: dev

server:
  port: 8089

logging:
  level:
    com.mediatheque: DEBUG
    org.hibernate.SQL: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html